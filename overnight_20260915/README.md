# 2026-09-15 夜间优化

北京时间 06:00 前优化面型与实际玻璃材料；06:00 至 08:30 将六片全部换为 CDGM 目录玻璃并优化。08:30 后只做原生验证和报告制作。

保持 A6 上限、105°完整视场、固定清口径与共同像面，检查全部玻璃边缘及跨光阑空气间隙。候选按材料组合保存不可变 ZMX 与指标，CDGM 另保留性能及目录相对成本的 Pareto 候选。目录相对成本不是厂家人民币报价，也不能作为跨厂降本百分比。

后台程序为 `scripts/overnight_supervisor.py`，运行记录为本目录 `supervisor_status.json`。每小时 00、15、30、45 分的当前任务自动跟进用于检查运行、恢复故障、制作及核验最终报告。不要重复开启第二套八实例优化池。

两个阶段分别保存在 `general/` 与 `cdgm/`。以 `phase_freeze.json` 和 `deadline_source.zmx` 为截止时的冻结版本；报告必须使用同一源文件 SHA 的原生验证数据。最终文件、报告和对比说明会在核验后推送到 GitHub。
